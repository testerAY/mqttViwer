# ✅ 機能追加・改修 ToDoリスト

## Phase 1: ウィジェット設定UIの実装 (Modal)
既存の `SettingsModal.vue` とは別に、ウィジェット設定専用のモーダルまたは共通化を行います。

- [x] **設定メニュー（3点リーダー）の追加** `[src/components/dashboard/WidgetHost.vue]`
    - カード右上にメニューボタンを配置。
    - メニュー項目：「設定」「CSVエクスポート」「削除」
- [x] **ウィジェット設定モーダルの作成**
    - `WidgetSettingsModal.vue` を新規作成（または既存モーダルを拡張）。
    - 以下のタブまたはセクションを設ける：
        - **基本設定**: タイトル、トピック
        - **データマッピング**: JSONキーの指定（例: x軸=timestamp, y軸=value）
        - **描画・スタイル**: 色、フォントサイズ、Min/Max値
        - **レイアウト**: 座標(x, y)・サイズ(w, h)の数値入力
- [x] **ストア連携**
    - モーダルでの変更内容を `useDashboardStore` に反映させるアクションの実装。

## Phase 2: バックエンド・データ処理の高度化 (Rust/DB)
受信ループ（MQTT通信）には負荷をかけず、DB保存プロセス（別スレッド）でデータの解析を行います。

- [ ] **DBスキーマの検討・更新** `[src-tauri/src/database.rs]`
    - 必要に応じて、データの型情報を保存するカラム（`data_type` 等）や、パース済みデータを検索しやすくする工夫（SQLiteのGenerated ColumnsやJSON拡張の利用検討）を追加。
- [ ] **ペイロード解析ロジックの実装** `[src-tauri/src/mqtt.rs]`
    - DB保存用の非同期タスク（`mpsc::channel` の受信側）内に解析処理を追加。
    - 入力テキストが「数値」か「JSON」かを判定。
    - JSONの場合、パースして構造を確認（バリデーション）。
- [ ] **履歴取得APIの改修** `[src-tauri/src/commands.rs]`
    - フロントエンドがチャート描画用に過去データを要求した際、解析済みの構造（あるいはJSONとして扱いやすい形式）で返却するように `get_history` コマンドを調整。

## Phase 3: 可視化ウィジェットのJSON対応
バックエンドに保存されたデータおよびリアルタイムデータを、設定に基づき描画します。

- [ ] **JSONデータ抽出ロジック (フロントエンド)**
    - リアルタイム受信データ（Raw String）を、設定されたキー（例: `payload.sensor.temp`）に基づいて値を取り出すユーティリティ関数の実装。
    - ※DB保存とは別に、表示用にはその場でパースが必要です。
- [ ] **チャートウィジェットの多機能化** `[src/components/widgets/ChartWidget.vue]`
    - 設定モーダルで指定された「X軸キー」「Y軸キー」を使ってデータをプロットするように改修。
    - 複数系列（Multi-series）の対応検討。
- [ ] **値表示・ゲージウィジェットの改修**
    - JSON内の特定キーの値をターゲットに表示できるよう改修。

## Phase 4: ウィジェット追加とドラッグ＆ドロップ
- [ ] **サイドバー（パレット）の作成**
    - 画面左側にドラッグ可能なウィジェットアイコン一覧を表示。
- [ ] **ドロップイベントの処理** `[src/components/dashboard/DashboardGrid.vue]`
    - グリッド上へのドロップを検知し、ドロップ位置の座標を取得。
    - 新規ウィジェットインスタンスを生成してストアに追加。

## Phase 5: 制御系ウィジェットの拡張 (Publish)
- [ ] **Publishコマンドの整備**
    - フロントエンドから任意のトピックへメッセージを送信する処理の共通化。
- [ ] **スライダー (Slider Widget)**
    - 新規作成。範囲（Min/Max/Step）設定と、値変更時のPublish実装。
- [ ] **スイッチ・ボタン拡張**
    - ON/OFF時の送信ペイロードをカスタマイズ可能にする。

## Phase 6: CSVエクスポート
- [ ] **エクスポート処理の実装**
    - 指定ウィジェットのトピック・期間のデータをDBから取得。
    - JSONデータの場合は、フラットなCSV形式（列展開）にするか、Rawデータのままにするかを選択可能にするとベター。
- [ ] **ファイル保存**
    - `tauri-plugin-dialog` を使用して保存先を指定し、ファイル書き出し。
