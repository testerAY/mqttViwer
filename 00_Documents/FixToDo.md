# MQTTブローカー・クラッシュ修正タスク

MQTTブローカー（rumqttd）の内部通信切断および、それに伴うアプリケーションの異常終了を解消するためのタスクリストです。

## 1. 依存関係の更新と整理
- [x] `src-tauri/Cargo.toml` 内の `rumqttd` のバージョン（現在 0.18.0）を、`rumqttc` (0.22.0) と互換性のある最新バージョンに更新する
- [x] `tokio` や `sqlx` などの主要ライブラリとのランタイム不整合がないか、`Cargo.lock` を含めて再確認する

## 2. ブローカー起動アーキテクチャの刷新
- [x] `src-tauri/src/broker.rs` の `start_broker` を `std::thread::spawn` によるスレッド起動から、Tauriの非同期ランタイム管理（`tauri::async_runtime::spawn`）に移行する
- [x] ブローカーの起動を `async fn` 化し、Tauriの `setup` フック内で他のコンポーネントと同期的に、あるいは適切な順序で初期化されるように変更する
- [x] `broker.start()` が返す結果を監視し、内部タスクが終了した際に適切に再起動またはエラーログを出力する仕組みを導入する

## 3. データベース書き込みの非同期化（ブロッキング防止）
- [x] `src-tauri/src/mqtt.rs` のイベントループ内で直接 `sqlx` を `await` 実行している箇所を修正する
- [x] `tokio::sync::mpsc` チャネルを導入し、MQTTで受信したメッセージを一旦キュー（バッファ）に入れる仕組みを構築する
- [x] DB書き込み専用のバックグラウンドタスクを作成し、MQTTイベントループを止めずにメッセージを永続化する

## 4. エラーハンドリングと可観測性の向上
- [x] `broker.rs` および `mqtt.rs` 内の `println!` / `eprintln!` を、プロジェクトで導入済みの `tracing` マクロ（`error!`, `warn!`, `info!`）に置き換える
- [x] `rumqttd` 内部の挙動を詳細に追跡するため、環境変数等でログレベルを `DEBUG` に切り替えられるようにする
- [ ] フロントエンドへの `emit` 処理がボトルネックにならないよう、大量メッセージ受信時のスロットリング（流量制限）を検討する

## 5. リソース制限の最適化
- [x] `broker.rs` 内の `max_outgoing_packet_count`（現在 1024）や `max_connections`（現在 100）の設定値が、実際の負荷に対して十分か再評価し、必要に応じて引き上げる
- [x] `router_config` の各パラメータを外部設定ファイルまたは定数として定義し、調整しやすくする
